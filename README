# 基于 Transformer 的英译中机器翻译系统

本项目实现课程作业要求的英译中机器翻译系统：输入一句英文，输出对应中文翻译。实现使用 PyTorch 与 Hugging Face `transformers`，默认基础模型为 `Helsinki-NLP/opus-mt-en-zh`。该模型是 Transformer 编码器-解码器结构的 MarianMT 翻译模型。

## 作业要求

- 任务：利用 Transformer 将输入英文句子翻译成中文。
- 输入示例：`tom is a student .`
- 输出示例：`汤姆 是 个 学生 。`
- 训练集：18000 句。
- 验证集：500 句。
- 测试集：2636 句。
- 实现框架：PyTorch。

## 数据集

数据集下载地址：

- 链接：`https://pan.baidu.com/s/1Vb3PvFkfCvJ_JdapgEU_Hg`
- 提取码：`h43i`

下载并解压后，将数据放到项目根目录的 `data/` 文件夹下。

推荐目录形式之一：

```text
TransTrans/
├── data/
│   └── cmn-eng-simple/
│       ├── training.txt
│       ├── validation.txt
│       └── testing.txt
```

每行推荐使用如下格式：

```text
english sentence<TAB>中文翻译
```

程序也兼容以下常见格式：

- `train.en` + `train.zh`、`val.en` + `val.zh`、`test.en` + `test.zh`
- `train.json`、`val.json`、`test.json`
- `train.jsonl`、`val.jsonl`、`test.jsonl`
- 单个包含全部句对的 `.txt`、`.tsv`、`.csv`、`.json` 或 `.jsonl` 文件

如果数据已经分好训练、验证、测试文件，程序会分别截取 `18000/500/2636` 条；如果只有一个总文件，程序会按顺序切分为训练集、验证集和测试集。

当前网盘数据集的实际文件名为 `training.txt`、`validation.txt`、`testing.txt`，程序会自动递归查找 `data/` 下的子目录并识别这些文件。数据中的 `@@` 子词标记会在加载时自动还原，例如 `re@@ sted` 会转成 `rested`。

## 项目结构

```text
TransTrans/
├── dataload.py       # 作业数据集加载与固定划分
├── train.py          # Transformer 翻译模型微调训练
├── train_custom.py   # 从零训练的 PyTorch Transformer
├── evaluate.py       # 测试集评估
├── inference.py      # 单句翻译推理
├── environment.yml   # Conda 环境
├── README            # 项目说明
├── data/             # 数据集目录，需手动下载放入
└── out/              # 模型、测试集、指标和分析结果，运行后生成
```

## 环境配置

使用 Conda 创建环境：

```bash
conda env create -f environment.yml
conda activate trans
```

如果只需要手动安装核心依赖，至少需要：

```bash
pip install torch transformers datasets sacrebleu sentencepiece matplotlib numpy
```

## 训练

确认数据已放入 `data/` 后运行：

```bash
python train.py
```

训练脚本会执行以下步骤：

1. 加载课程英译中数据集。
2. 固定构造 `18000` 条训练样本、`500` 条验证样本、`2636` 条测试样本。
3. 使用 MarianMT Transformer 模型进行英译中微调。
4. 保存模型到 `out/model/`。
5. 保存测试集到 `out/data/test_samples.json`。
6. 保存数据划分信息到 `out/data/split_meta.json`。

默认训练参数在 `train.py` 的 `TrainConfig` 中：

| 参数 | 默认值 |
| --- | --- |
| `model_name` | `Helsinki-NLP/opus-mt-en-zh` |
| `data_dir` | `data` |
| `train_size` | `18000` |
| `val_size` | `500` |
| `test_size` | `2636` |
| `batch_size` | `8` |
| `eval_batch_size` | `8` |
| `learning_rate` | `3e-5` |
| `num_train_epochs` | `3` |
| `generation_num_beams` | `4` |
| `progress_log_steps` | `50` |

训练日志会额外输出总进度，例如：

```text
[progress] step=500/6750 (7.41%), epoch=0.2222, speed=1.250 step/s, elapsed=6m 40s, segment_elapsed=40s
```

后台运行时可用下面的命令查看：

```bash
tail -f logs/train.log
```

## 从零训练自建 Transformer

如果不使用预训练模型，可以运行独立脚本：

```bash
python train_custom.py
```

该脚本使用 `torch.nn.Transformer` 搭建编码器-解码器模型，随机初始化参数，并读取课程数据集自带的 `word2int_en.json`、`word2int_cn.json`、`int2word_cn.json` 词表。默认模型规模为 3 层、`d_model=256`、4 个注意力头，输出保存到：

```text
out_custom/
├── custom_transformer.pt
├── metrics.json
├── predictions.json
├── run_config.json
└── test_samples.json
```

快速检查流程可以先跑小样本：

```bash
python train_custom.py --train-size 64 --val-size 16 --test-size 16 --epochs 1 --batch-size 16 --d-model 64 --num-layers 1 --dim-feedforward 128
```

CPU 上从零训练会明显慢于微调版，效果也通常低很多；建议先用小模型确认流程，再逐步增加 `--epochs`、`--d-model` 和训练样本量。

## 评估

训练完成后运行：

```bash
python evaluate.py
```

评估脚本会读取：

- 模型：`out/model/`
- 测试集：`out/data/test_samples.json`

输出指标包括：

- BLEU
- ROUGE-1
- ROUGE-2
- ROUGE-L
- METEOR
- chrF

评估结果保存到：

```text
out/eval/evaluation_results.json
```

## 推理

训练完成后运行：

```bash
python inference.py
```

脚本会加载 `out/model/` 并对内置英文示例进行翻译。也可以在 Python 中直接调用：

```python
from inference import TranslationInference

translator = TranslationInference()
print(translator.translate("tom is a student ."))
```

## 输出文件

```text
out/
├── model/                         # 微调后的模型和 tokenizer
├── checkpoints/                   # 训练过程检查点
├── data/
│   ├── test_samples.json          # 测试集
│   └── split_meta.json            # 数据集划分信息
├── eval/
│   └── evaluation_results.json    # 测试集评估指标
└── analysis/
    ├── run_config.json            # 本次训练配置
    ├── train_metrics.json         # 训练汇总指标
    ├── training_history.json      # 训练过程日志
    ├── train_loss_curve.png       # 训练损失曲线
    ├── val_loss_curve.png         # 验证损失曲线
    └── val_bleu_curve.png         # 验证 BLEU 曲线
```

## 说明

本项目默认优先读取本地 `data/` 目录中的课程数据集。若没有找到本地数据，`dataload.py` 会尝试使用 Hugging Face WMT17 英中数据作为兜底，便于检查代码流程；正式提交课程作业时应使用网盘提供的数据集。
