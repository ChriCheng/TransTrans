# 基于 Transformer 的中英翻译实验报告

## 1. 实验概述

本实验基于 PyTorch 实现一个标准的 Transformer 编码器-解码器翻译模型，完成一个小规模中英翻译任务。仓库当前包含模型实现、训练脚本、评估脚本和推理脚本，目标是通过最小可运行系统验证 Transformer 在序列到序列任务中的基本工作流程。


## 2. 实验目标

本实验的目标如下：
- 从零实现 Transformer 的核心模块。
- 构建一个最小中英平行语料训练流程。
- 完成模型训练、保存、加载与评估。
- 观察模型在小数据场景下的收敛行为与翻译表现。
- 分析实现缺陷对生成质量的影响，并完成修复验证。

## 3. 实验环境

### 3.1 软件环境

- Python 3.11
- PyTorch 2.11.0
- torchvision 0.26.0
- torchaudio 2.11.0
- numpy 2.4.4
- tqdm 4.67.3
- 其余依赖见 `requirement.txt`

### 3.2 硬件环境

本次实际训练日志显示运行设备为 `cuda`。

## 4. 项目结构

```text
TransTrans/
├── transformer_model.py              # Transformer 模型实现
├── train.py                          # 训练脚本
├── evaluate.py                       # 评估脚本
├── inference.py                      # 推理脚本
├── requirement.txt                   # 依赖列表
├── Transformer_Translation_Report.md # 原始课程报告
├── out/                              # 模型权重、词表、评估结果输出目录
└── log/                              # 训练与评估日志目录
```

## 5. 模型原理与实现

### 5.1 整体结构

模型采用标准 Transformer Encoder-Decoder 架构，主要由以下部分组成：
- 词嵌入层
- 正弦位置编码
- 多头自注意力机制
- 前馈神经网络
- 编码器层堆叠
- 解码器层堆叠
- 线性输出层

### 5.2 核心模块

#### 5.2.1 位置编码

由于 Transformer 不具备循环结构，因此需要通过位置编码显式注入序列顺序信息。项目中使用正弦-余弦位置编码。

#### 5.2.2 多头注意力

模型通过多头注意力机制并行建模不同子空间中的依赖关系。编码器使用自注意力，解码器同时使用自注意力和交叉注意力。

#### 5.2.3 因果掩码

在解码阶段，为了保证第 `t` 个位置只能看到当前及之前的 token，目标序列必须使用因果掩码。该掩码已在当前训练和评估逻辑中启用。

## 6. 数据集与预处理

当前仓库使用的是一个极小规模的示例平行语料，共 8 组样本：

| 英文 | 中文 |
|------|------|
| hello world | 你好 世界 |
| good morning | 早上 好 |
| how are you | 你 好 吗 |
| thank you very much | 非常 感谢 你 |
| what is your name | 你 叫 什么 名字 |
| i love machine learning | 我 喜欢 机器 学习 |
| transformer is powerful | transformer 很 强大 |
| neural networks are amazing | 神经 网络 很 棒 |

预处理流程如下：
- 对输入文本执行 `lower()`。
- 按空格进行切分。
- 构建源语言和目标语言词表。
- 引入特殊符号 `<PAD>`、`<UNK>`、`<SOS>`、`<EOS>`。
- 将序列填充或截断到最大长度 `100`。

修复后，目标序列在训练数据中显式加入 `<SOS>`，从而与推理过程保持一致。

## 7. 实验设置

### 7.1 模型超参数

| 参数 | 取值 |
|------|------|
| `d_model` | 256 |
| `num_layers` | 4 |
| `num_heads` | 8 |
| `d_ff` | 1024 |
| `dropout` | 0.1 |
| `max_seq_length` | 100 |
| `batch_size` | 4 |
| `learning_rate` | 0.0001 |
| `num_epochs` | 20 |

### 7.2 参数规模

训练日志显示模型参数量为：

- `7,392,537`

## 8. 关键修复记录

本仓库原始版本在训练与评估逻辑中存在几个会直接影响生成质量的问题。

### 8.1 目标序列缺少 `<SOS>`

原始训练数据仅在末尾加入 `<EOS>`，但推理时从 `<SOS>` 开始生成，导致训练分布和推理分布不一致。当前版本已修复，训练目标序列开头会显式插入 `<SOS>`。

### 8.2 缺少 decoder 因果 mask

原始训练和验证阶段未向解码器传入因果掩码，导致模型训练时能够“偷看未来 token”，从而造成训练行为与真实自回归推理不一致。当前版本在训练和评估中均启用了 `generate_tgt_mask()`。

### 8.3 评估阶段错误从 PAD 位置取预测结果

原始 `evaluate.py` 中会先把目标输入补齐到源序列长度，再对 `decoder_output[:, -1, :]` 取 logits。由于最后一个位置多数时候是 PAD，这会导致生成结果经常为空或不断重复同一个词。当前版本已经改为只对已生成长度做解码，并从最后一个真实位置取预测结果。

### 8.4 CrossEntropyLoss 忽略索引错误

原始损失函数使用的是 `src_vocab['<PAD>']` 作为 `ignore_index`，当前版本已修复为 `tgt_vocab['<PAD>']`。

## 9. 训练结果

本次修复后重新训练，训练日志中的损失下降如下：

| Epoch | Loss |
|------|------|
| 1 | 3.2245 |
| 2 | 2.8757 |
| 3 | 2.5951 |
| 4 | 2.3332 |
| 5 | 2.1601 |
| 6 | 1.9772 |
| 7 | 1.6966 |
| 8 | 1.5684 |
| 9 | 1.2487 |
| 10 | 1.0408 |
| 11 | 0.9653 |
| 12 | 0.8683 |
| 13 | 0.7208 |
| 14 | 0.5892 |
| 15 | 0.5169 |
| 16 | 0.4477 |
| 17 | 0.3769 |
| 18 | 0.3249 |
| 19 | 0.2963 |
| 20 | 0.2521 |

可以看到模型在该小规模数据集上稳定收敛。

## 10. 评估结果

修复后重新运行 `evaluate.py`，得到如下结果：

| 源文本 | 参考翻译 | 模型翻译 |
|------|------|------|
| hello world | 你好 世界 | 你好 世界 |
| good morning | 早上 好 | 早上 好 |
| how are you | 你 好 吗 | 你 好 吗 |
| thank you very much | 非常 感谢 你 | 非常 感谢 你 |
| i love machine learning | 我 喜欢 机器 学习 | 我 喜欢 机器 学习 |
| transformer is powerful | transformer 很 强大 | transformer 很 强大 |
| neural networks are amazing | 神经 网络 很 棒 | 神经 网络 很 棒 |

平均指标如下：

| 指标 | 数值 |
|------|------|
| BLEU | 0.2857 |
| ROUGE-L | 1.0000 |
| METEOR | 0.8214 |

### 10.1 指标说明

- `ROUGE-L = 1.0000` 说明在当前测试集上，预测序列与参考序列完全一致。
- `METEOR = 0.8214` 反映出较高的匹配程度。
- `BLEU = 0.2857` 偏低并不表示翻译错误，而是因为当前 `compute_bleu()` 实现未做平滑处理。对于短句，当高阶 n-gram 不存在时，会直接得到 0 分。因此当前 BLEU 实现不适合用于这种极小短句测试集的严格比较。

## 11. 结果分析


### 11.3 当前结论

在当前这个极小规模训练集上，Transformer 已经能够完成记忆式拟合并正确输出训练集中的翻译样本。实验目的已经达成，即验证了从零实现 Transformer 训练、解码、评估链路的可行性。

## 12. 当前仓库的局限

虽然训练和评估链路已经修复，但仓库仍存在以下现实限制：

- 数据集极小，仅适合教学演示，不具备泛化能力。
- 分词方式是空格切分，没有使用 BPE、WordPiece 等子词方法。
- `inference.py` 仍保留旧的贪心解码和束搜索逻辑，尚未同步本次修复，因此交互推理脚本与 `evaluate.py` 的当前正确解码策略并不完全一致。
- BLEU 实现较为简化，缺少平滑项，不适合作为小样本短句任务的主要评价依据。

## 13. 后续改进方向

建议下一步按以下顺序继续完善：

1. 同步修复 `inference.py` 的贪心解码和束搜索逻辑。
2. 替换当前简化版 BLEU 为带平滑的标准实现，例如 `sacrebleu`。
3. 引入更大规模的中英平行语料，验证模型的真实泛化能力。
4. 加入验证集、早停、学习率调度等训练策略。
5. 使用子词分词方法提升未登录词处理能力。
6. 增加注意力可视化或 Web 界面，提升实验展示效果。

## 14. 运行方式

### 14.1 训练

```bash
python3 train.py
```

### 14.2 评估

```bash
python3 evaluate.py
```

### 14.3 依赖安装

```bash
pip install -r requirement.txt
```

## 15. 总结

本实验完成了一个从零实现的 Transformer 中英翻译系统，并在当前仓库基础上修复了训练-推理不一致、缺少因果掩码、错误解码位置和损失忽略索引等关键问题。修复后模型在当前小规模测试集上能够稳定输出正确翻译，说明整体实现链路已经具备正确性。

如果后续继续补齐 `inference.py`、标准化评估指标并扩展语料规模，该仓库可以从课程演示版本进一步发展为更完整的实验基线。
