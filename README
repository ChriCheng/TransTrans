# 基于 Transformer 的中英翻译实验报告

## 1. 实验概述

本实验围绕机器翻译任务，完成了一个基于 Transformer 思想的英译中翻译系统实现与验证。项目仓库包含模型结构文件、训练脚本、评估脚本和推理脚本，形成了从数据加载、模型微调、结果评估到单句翻译推理的完整实验流程。

 `train.py`、`evaluate.py` 和 `inference.py` 构成了项目当前真正可运行的主实验链路，最初我尝试手搭架构，但是最后测试的结果极差，最后基于 Hugging Face 提供的预训练 MarianMT 模型 `Helsinki-NLP/opus-mt-en-zh`，在 WMT17 英中平行语料上进行微调取得了不错的结果。



---

## 2. 实验目标

本实验的主要目标如下：

- 理解 Transformer 编码器—解码器结构的基本原理；
- 掌握位置编码、多头注意力、前馈网络与掩码机制的作用；
- 构建一个最小可运行的英译中翻译流程；
- 完成模型训练、保存、加载、推理与评估；
- 观察小规模训练条件下翻译模型的收敛行为与生成效果；
- 分析当前项目结构中“手写 Transformer 模块”和“预训练微调主链路”之间的关系，并总结其优缺点。

---

## 3. 实验环境

### 3.1 软件环境

根据项目中的 `requirement.txt` 和 `environment.yml`，本实验使用的主要软件环境如下：

- Python 3.11
- PyTorch 2.11.0
- torchvision 0.26.0
- torchaudio 2.11.0
- transformers 5.5.1
- datasets 4.8.4
- sacrebleu 2.6.0
- numpy 2.4.4
- tqdm 4.67.3
- sentencepiece 0.2.x
- accelerate 1.x

若采用 Conda，可直接使用项目中的 `environment.yml` 进行环境复现。

### 3.2 硬件环境

项目中的评估脚本和推理脚本均采用自动设备选择机制：

- 若检测到 CUDA，可优先使用 GPU；
- 若无可用 GPU，则自动回退到 CPU。

因此，本项目既可以在单 GPU 环境中运行，也可以在 CPU 环境下完成功能验证，只是训练速度会明显变慢。

---

## 4. 项目结构

```text
TransTrans/
├── dataload.py                        # 数据加载与辅助处理脚本
├── environment.yml                   # Conda 环境配置文件
├── evaluate.py                       # 模型评估脚本
├── inference.py                      # 单句推理脚本
├── requirement.txt                   # Python 依赖列表
├── train.py                          # MarianMT 微调训练脚本
├── Transformer_Translation_Report.md # 原始课程报告
├── log/                              # 日志记录
└── out/                              # 训练输出目录（运行后生成）
    ├── model/                        # 最终模型与 tokenizer
    ├── checkpoints/                  # 训练中间检查点
    ├── data/                         # 测试集与数据划分信息
    ├── eval/                         # 评估结果
    └── analysis/                     # 曲线图、训练历史、分析文件

```

当前输出目录按照用途进行了归档，运行后可重点关注以下位置：

- `out/model/`：训练完成后的最终模型、tokenizer 和推理所需配置文件；
- `out/checkpoints/`：训练过程中的阶段性 checkpoint，用于断点续训或回看中间结果；
- `out/data/`：数据划分信息与测试集样本，例如 `split_meta.json`、`test_samples.json`；
- `out/eval/`：离线评估结果，例如 `evaluation_results.json`；
- `out/analysis/`：训练与验证曲线、历史指标、trainer 状态等分析产物。

旧版手写 Transformer 链路遗留的 `model.pth`、`src_vocab.json`、`tgt_vocab.json` 已不再作为当前主实验流程输出，也不再写入 `out/` 根目录。



## 5. 模型原理与实现

### 5.1 整体结构

从机器翻译任务的角度看，Transformer 是一种标准的序列到序列模型。其核心思想是使用编码器对输入序列进行表示学习，再由解码器根据编码结果和先前已生成的目标序列进行自回归生成。
### 5.2 核心模块

#### 5.2.1 位置编码

由于 Transformer 不具备循环结构，也没有卷积网络那样的局部顺序归纳偏置，因此必须通过显式方式将位置信息注入模型。项目中采用的是经典的正弦—余弦位置编码方法。

位置编码的优点主要体现在以下几个方面：

- 不需要额外学习参数；
- 能够为不同位置生成稳定的表示；
- 有助于模型学习绝对位置和相对位置关系。

在实现上，位置编码首先构造长度为 `max_seq_length`、维度为 `d_model` 的矩阵，然后通过正弦函数填充偶数维，通过余弦函数填充奇数维，最后与词向量相加后送入后续网络。

#### 5.2.2 多头注意力

多头注意力是 Transformer 的核心组成。其基本思想是：将输入分别映射为 Query、Key 和 Value，在多个不同子空间中并行计算注意力结果，最后拼接并线性变换输出。

项目中的 `MultiHeadAttention` 模块包含四个线性层：

- `W_q`
- `W_k`
- `W_v`
- `W_o`

其基本计算流程如下：

1. 对输入做线性投影，得到 Q、K、V；
2. 将隐藏维度拆分为多个 head；
3. 对每个 head 分别计算缩放点积注意力；
4. 将各 head 输出拼接后再做一次线性映射。

这种机制的优势在于：

- 可以同时关注句子中不同位置之间的依赖关系；
- 可以从不同表示子空间中抽取不同类型的语义信息；
- 对长距离依赖建模能力强。

#### 5.2.3 因果掩码

在解码阶段，模型生成当前位置的词时，不应该看到未来位置的真实词，否则就会造成信息泄漏。为此，Transformer 在解码器自注意力中引入因果掩码。

项目中的 `generate_tgt_mask()` 函数同时结合了两种掩码：

1. **padding mask**：忽略补齐位置；
2. **causal mask**：只允许当前位置关注自己及之前的位置。

这一步对于保证自回归生成的正确性非常关键。

#### 5.2.4 前馈网络与残差归一化

每个编码器层和解码器层中，除了注意力子层外，还包含一个前馈网络。前馈网络对每个位置独立施加非线性变换，以提升模型表示能力。

同时，每个子层之后还包含：

- 残差连接；
- LayerNorm 层归一化；
- Dropout。

这种设计能够稳定训练过程，缓解深层网络的梯度问题。

---

## 6. 数据集与预处理

### 6.1 数据来源

项目主训练脚本中，数据来自 Hugging Face `datasets` 提供的 `wmt/wmt17` 数据集，配置为 `zh-en`。但在实际训练时，脚本选择其中的英文作为源语言、中文作为目标语言，因此实际任务方向为：

**English → Chinese（英译中）**

默认情况下，脚本从训练集前部截取 `8000` 条样本用于实验。

### 6.2 数据清洗策略

为了保证语料质量，训练脚本在加载原始样本后执行了一系列启发式过滤，包括：

- 去除空字符串；
- 判断源句是否主要由英文构成；
- 判断源句是否符合基本英文句子形式；
- 判断目标句是否主要由中文字符构成；
- 过滤过短或过长样本；
- 过滤中英长度比例异常的样本；
- 去除重复样本。

这一步能够提升训练数据的一致性，减少噪声样本对模型的干扰。

### 6.3 数据划分

清洗完成后，数据被随机打乱，并按以下比例划分：

- 训练集：80%
- 验证集：10%
- 测试集：10%

测试集文本会被保存为 `out/data/test_samples.json`，以便后续评估脚本统一读取。

### 6.4 分词与编码

由于当前主训练链路使用的是 MarianMT 预训练模型，因此不再手动构建词表，也不再自行实现 `<PAD>`、`<SOS>`、`<EOS>` 等特殊符号管理逻辑，而是直接调用预训练模型对应的 tokenizer 进行编码。

训练时主要包括两部分长度约束：

- `max_source_length = 128`
- `max_target_length = 160`

这与项目中手写 Transformer 路线下“自行构建词表与 token 映射”的思路不同，也体现出工程化实现与教学化实现之间的差异。

---

## 7. 实验设置

### 7.1 模型与训练配置

根据 `train.py` 中定义的 `TrainConfig`，本实验默认采用如下设置：

| 参数                    | 取值                         |
| ----------------------- | ---------------------------- |
| `model_name`            | `Helsinki-NLP/opus-mt-en-zh` |
| `num_examples`          | 8000                         |
| `max_source_length`     | 128                          |
| `max_target_length`     | 160                          |
| `batch_size`            | 8                            |
| `eval_batch_size`       | 8                            |
| `learning_rate`         | 3e-5                         |
| `num_train_epochs`      | 3                            |
| `weight_decay`          | 0.01                         |
| `warmup_ratio`          | 0.1                          |
| `generation_max_length` | 160                          |
| `generation_num_beams`  | 4                            |
| `no_repeat_ngram_size`  | 3                            |
| `repetition_penalty`    | 1.2                          |
| `seed`                  | 42                           |

可以看出，当前主实验流程更偏向于“在小规模样本上进行快速微调验证”，而不是训练一个大规模从零开始的翻译模型。

### 7.2 训练策略

训练阶段使用 Hugging Face 的 `Seq2SeqTrainer`，并采用如下策略：

- 每个 epoch 进行一次验证；
- 每个 epoch 保存一次模型；
- 使用 `predict_with_generate=True` 在验证阶段直接生成文本进行评估；
- 以 `eval_bleu` 作为选择最佳模型的指标；
- 保留最多两个检查点；
- 训练结束后自动加载最佳模型。

这一设计使得训练、验证和模型选择流程较为完整，也适合课程实验和小型项目复现。

### 7.3 生成策略

在评估和推理阶段，项目采用 beam search 进行生成，主要参数包括：

- `num_beams = 4`
- `no_repeat_ngram_size = 3`
- `repetition_penalty = 1.2`
- `early_stopping = True`

相比简单贪心解码，这样的设置能够在一定程度上减少重复生成问题，并提升翻译结果的整体可读性。

---

## 8. 训练流程说明

整个实验流程可以概括为以下几个步骤：

1. 加载 MarianMT 预训练模型与对应 tokenizer；
2. 下载并读取 WMT17 英中平行语料；
3. 对原始样本进行语言检测、长度过滤和去重清洗；
4. 将数据划分为训练集、验证集和测试集；
5. 对文本进行 tokenizer 编码；
6. 使用 `Seq2SeqTrainer` 执行微调训练；
7. 保存最佳模型、配置文件与测试集信息；
8. 调用评估脚本对测试集进行翻译并计算 BLEU；
9. 调用推理脚本对单句输入进行翻译展示。

这一流程已经构成了一个较完整的机器翻译实验闭环。

---

## 9. 训练结果

### 9.1 当前仓库中的结果状态

需要如实说明的是，当前 GitHub 仓库本身并未提交 `out/analysis/train_metrics.json`、`out/eval/evaluation_results.json` 等运行结果文件，因此无法直接从仓库静态内容中读取具体训练损失曲线、验证 BLEU 数值或测试集最终得分。

也就是说，项目代码已经具备完整的训练与评估功能，但仓库当前公开内容更偏向“代码实现与实验框架展示”，而不是“附带完整实验产物的结果仓库”。

### 9.2 预期训练现象

在当前设置下，训练样本量仅为 8000 条，训练轮数为 3，且基础模型已经具有较强的翻译能力，因此通常可以预期出现以下现象：

- 训练过程较快收敛；
- 验证 BLEU 相比未微调模型会出现一定提升或域适应效果；
- 对常见、短句、结构清晰的英文输入能够生成较自然的中文；
- 对长句、复杂句、低频表达的处理能力仍受数据规模限制。

如果后续本地完成训练，可将 `out/analysis/train_metrics.json` 中的 loss 和验证指标补充到本节中，进一步完善实验报告。

---

## 10. 评估结果

### 10.1 评估方式

项目中的 `evaluate.py` 会执行以下流程：

1. 从 `out/model/` 目录加载微调后的模型和 tokenizer；
2. 读取 `out/data/test_samples.json` 中保存的测试集；
3. 对每条测试源句进行生成；
4. 打印前若干条样本的源句、参考翻译和模型翻译；
5. 使用 `sacrebleu.corpus_bleu()` 计算整个测试集的 Corpus BLEU；
6. 将结果保存到 `out/eval/evaluation_results.json`。

因此，该评估方式既保留了自动指标，也保留了可人工检查的样例输出。

### 10.2 指标说明

本项目当前主要使用的自动评估指标为 **Corpus BLEU**。

BLEU 的基本思想是比较模型翻译结果与参考翻译在 n-gram 层面的重合程度，并结合长度惩罚项，评估整体翻译质量。其特点包括：

- 适合对整个测试集进行整体评价；
- 便于不同模型或不同实验设置之间做横向对比；
- 对于小规模数据集上的教学实验，计算方便、解释直观。

需要注意的是，BLEU 更适合宏观比较，不一定能完全反映单句可读性或语义等价性。因此，在课程实验中最好同时结合具体样例进行分析。

### 10.3 当前结果说明

由于当前仓库未提交实际运行产生的 `evaluation_results.json` 文件，因此本报告不伪造具体 BLEU 数值。实际实验结果应以本地运行 `evaluate.py` 后生成的 `out/eval/evaluation_results.json` 为准。

如果后续已在本地得到结果，可将本节补写为如下形式：

```text
Corpus BLEU 分数: xx.xx
测试样本数: xxxx
```

并结合若干样本翻译结果进行进一步分析。

---

## 11. 结果分析

### 11.1 当前实现的优点

本项目具有以下几个明显优点：

**第一，结构清晰。**
仓库中手写 Transformer 模块和主实验脚本相对独立，既保留了对模型原理的展示，也保留了可直接运行的工程化实现。

**第二，实验闭环完整。**
从数据加载、清洗、划分，到模型训练、验证、保存，再到测试评估和单句推理，整个实验链路是完整的。

**第三，复现门槛较低。**
项目中提供了 `requirement.txt` 和 `environment.yml`，方便快速配置环境；同时 Hugging Face 框架显著简化了预训练模型的加载与训练流程。

**第四，适合课程展示。**
相较于完全从零训练一个翻译模型，基于 MarianMT 的微调更容易在有限算力和有限时间内获得可观测结果，更适合作为教学实验展示。


### 11.2 当前结论

综合来看，当前项目已经成功完成了一个小规模英译中翻译实验框架，且主链路是可运行、可评估、可推理的。虽然它并不是严格意义上“从零训练 Transformer 翻译模型”，但在课程实验的语境下，这一实现仍然具有较高的实用价值。

一方面，手写 Transformer 文件帮助理解模型原理；另一方面，MarianMT 微调链路确保了项目能够在有限时间和资源下真正跑通并得到结果。因此，该项目已经实现了“理论学习 + 工程验证”相结合的实验目标。

---





### 12.4 增强推理脚本的交互能力

当前 `inference.py` 使用固定测试句进行演示。后续可以改为：

- 命令行交互输入；
- 批量文本翻译；
- 文件输入输出；
- Web 页面交互。

这将提升项目的可展示性与可用性。

### 12.5 补充实验结果与可视化

建议在仓库中补充：

- 训练 loss 曲线；
- 验证 BLEU 曲线；
- 典型样例翻译结果；
- 实验配置说明。

这样能使项目从“可运行代码仓库”进一步提升为“结果完整的实验仓库”。

---

## 13. 总结

本实验围绕中英机器翻译任务，对一个基于 Transformer 思想的翻译项目进行了实现分析与实验整理。项目中既包含手写的 Transformer 编码器—解码器结构，也包含基于预训练 MarianMT 模型的英译中微调链路。

从实验实现角度看，当前仓库已经具备较完整的工程流程：能够完成数据加载、清洗、划分、模型训练、测试评估与单句推理。从教学角度看，项目中的手写 Transformer 模块也有助于理解位置编码、多头注意力、前馈网络和因果掩码等核心机制。

需要特别指出的是，当前主实验结果来自预训练模型微调，而不是直接来自手写 Transformer 模块。因此，在报告表述中应准确区分“原理实现”和“实际运行主链路”。

总体而言，本项目已经达到了课程实验的基本目标：既展示了 Transformer 的理论结构，又完成了一个可运行的英译中翻译系统验证。若后续进一步补齐手写模型训练闭环、扩展数据规模并补充完整实验结果，该项目还可以继续发展为更规范、更完整的机器翻译实验基线。

---

## 14. 项目地址

本项目已在 GitHub 开源：

`https://github.com/ChriCheng/TransTrans`

---

## 15. 参考文献

[1] Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A. N., Kaiser, Ł., & Polosukhin, I. Attention Is All You Need. *Advances in Neural Information Processing Systems*, 2017.

[2] Papineni, K., Roukos, S., Ward, T., & Zhu, W. J. BLEU: a Method for Automatic Evaluation of Machine Translation. *Proceedings of ACL*, 2002.

[3] Junczys-Dowmunt, M., Grundkiewicz, R., Dwojak, T., Hoang, H., Heafield, K., Neckermann, T., Seide, F., Germann, U., Aji, A. F., Bogoychev, N., Martins, A. F. T., & Birch, A. Marian: Fast Neural Machine Translation in C++. *Proceedings of ACL System Demonstrations*, 2018.
